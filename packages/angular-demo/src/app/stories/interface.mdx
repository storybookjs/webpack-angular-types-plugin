import { ArgTypes, Meta } from '@storybook/addon-docs/blocks';

<Meta title="TypeScript/Interfaces" />

# Interface

## Product

Interfaces need to be annotated with `@include-docs` for the type information to be generated.

> **Note**
>
> Type-only modules (TypeScript files that only contain `type`s and `interface`s) are eliminated
> during compilation and thus, cannot be analyzed.
>
> Add at least one **used** constant or function declaration to the file containing the interface.

<ArgTypes of="Product" />

## Aliases

Use `@include-docs` to specify aliases for the interface as identifiers.

```typescript
/**
 * @include-docs Cost,Charge
 */
export interface Price {
	/* ... */
}
```

### Price

<ArgTypes of="Price" />

### Cost

<ArgTypes of="Cost" />

### Charge

<ArgTypes of="Charge" />

## Without @include-docs

Interfaces without the `@include-docs` annotation are ignored.

### UndocumentedSecret

<ArgTypes of="UndocumentedSecret" />
