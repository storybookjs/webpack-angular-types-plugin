import { ArgTypes, Meta } from '@storybook/addon-docs';

<Meta title="TypeScript/Functions and constants" />
# Functions and constants
Functions and constants need to be annotated with `@include-docs` for the type information to be generated.

## Functions

<ArgTypes of="isNumber" />

<ArgTypes of="isString" />

## Constants

<ArgTypes of="IS_STANDALONE" />

## Aliases and grouping

Use the `@include-docs` annotation to

- specify aliases for the function and constants that can be used as `of` identifiers for the `ArgTypes` and/or
- group multiple exports into a single `ArgTypes` table.

```typescript
/**
 * Current environment
 *
 * @include-docs Env, LibraryInfo, EnvironmentInfo
 */
export const ENVIRONMENT = 'production';

/**
 * Returns the current environment
 *
 * @include-docs LibraryInfo, EnvironmentInfo
 */
export function getEnvironment(): string {
	// ...
}

```

### ENVIRONMENT

<ArgTypes of="ENVIRONMENT" />

### Env

<ArgTypes of="Env" />

### EnvironmentInfo

<ArgTypes of="EnvironmentInfo" />

### LibraryInfo

<ArgTypes of="LibraryInfo" />

### Single function

<ArgTypes of="getEnvironment" />

## Without @include-docs

Functions and constants without the `@include-docs` annotation are ignored.

### withoutAnnotation

<ArgTypes of="withoutAnnotation" />

### WITHOUT_ANNOTATION

<ArgTypes of="WITHOUT_ANNOTATION" />
